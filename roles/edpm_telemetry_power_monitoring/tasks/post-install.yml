---
- name: Ensure firewall directory is present
  become: true
  ansible.builtin.file:
    path: "/var/lib/edpm-config/firewall/"
    state: directory
    owner: root
    group: root
    mode: '0750'

- name: Copy ceilometer ipmi firewall config
  become: true
  ansible.builtin.copy:
    src: "firewall.yaml"
    dest: "/var/lib/edpm-config/firewall/ceilometer_ipmi.yaml"
    mode: "0640"

- name: Configure firewall for ceilometer ipmi
  ansible.builtin.include_role:
    name: osp.edpm.edpm_nftables
    tasks_from: "configure.yml"

- name: Reload firewall for ceilometer ipmi
  ansible.builtin.include_role:
    name: osp.edpm.edpm_nftables
    tasks_from: "run.yml"
